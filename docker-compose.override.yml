services:
  frontend:
    image: tracker-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
  backend:
    image: tracker-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
